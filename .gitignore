# Don't push this folder as it contains unnecessary files 
**/build
# Don't push the folder containing the tools and WIP stuff
tools/
# Don't push those as they are to be generated by Magikoopa anyway
linker.x
subsdk[0-9]
*.elf